# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  gpu: true
  cuda: "12.1"
  python_version: "3.11"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
    - "git"
    - "wget"
  python_packages:
    - "torch==2.1.0"
    - "torchvision==0.16.0"
    - "transformers==4.35.0"
    - "pillow>=10.0.0"
    - "numpy<2"
    - "trimesh>=4.0.0"
    - "rembg[gpu]>=2.0.50"
    - "omegaconf>=2.3.0"
    - "einops>=0.7.0"
    - "pygltflib>=1.16.0"
    - "imageio[ffmpeg]"
    - "xatlas"
    - "moderngl"
    - "scikit-image"
  run:
    - "git clone https://github.com/VAST-AI-Research/TripoSR.git /tmp/TripoSR && cp -r /tmp/TripoSR/tsr ."
    - "python -c 'from huggingface_hub import hf_hub_download; hf_hub_download(repo_id=\"stabilityai/TripoSR\", filename=\"config.yaml\"); hf_hub_download(repo_id=\"stabilityai/TripoSR\", filename=\"model.ckpt\")'"
    - "python -c 'from rembg import new_session; new_session(\"u2net\")'"

predict: "predict.py:Predictor"
